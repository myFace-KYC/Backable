const progressbar = {
  margin: "7% 0% 10% 0%"
};

const approverStyle = {
  margin: "0% 0% 0% 0%"
};

const addressOverflow = {
  textOverflow: "ellipsis",
  overflow: "hidden",
  padding: "0em 2em 0.7em 2em "
};

const containerSegment = {
  background: "#FFFFFF",
  boxShadow: "0 10px 50px 0 rgba(0,0,0,0.17)",
  borderRadius: "18px",
  padding: "1.1em"
};

const contributeSegment = {
  background: "#FFFFFF",
  boxShadow: "0 10px 50px 0 rgba(0,0,0,0.17)",
  borderRadius: "18px",
  padding: "2.8em"
};

const containerHeaderSegment = {
  background: "#FFFFFF",
  boxShadow: "0 10px 50px 0 rgba(0,0,0,0.17)",
  borderRadius: "18px",
  padding: "1.4em"
};

const singleSegment = {
  padding: "0.7em 2em 0.7em 2em "
};

const viewRequest = {
  margin: "1.1em 0em 0em 0em "
};

const addressSegment = {
  padding: "0.3em 0.8em 0.3em 0.8em ",
  border: "10px black"
};

const singleHeaderSegment = {
  padding: "0.3em 0.8em 0.3em 0.8em "
};
const picSegment = {
  background: "#FFFFFF",
  boxShadow: "0 14px 50px 0 rgba(0,0,0,0.15)",
  borderRadius: "18px"
};

export default {
  progressbar,
  approverStyle,
  addressOverflow,
  containerSegment,
  singleSegment,
  picSegment,
  singleHeaderSegment,
  addressSegment,
  containerHeaderSegment,
  contributeSegment,
  viewRequest
};
